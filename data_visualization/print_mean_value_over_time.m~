clear all
clc

d = dlmread('../data_bci/sp1s_aa_train.txt');

output = d(:,1);
d = d(:,2:end);

data_channels = {};
number_channels = 28;
n_times = 50;

for i = 1:number_channels
   data_channels{end+1} = d(:,(i-1)*n_times+1:i*n_times);
end

mean(mean(data_channel{1}